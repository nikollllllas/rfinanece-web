/** Nome do cookie no domínio do front (valor = JWT emitido pela API). */
export const AUTH_COOKIE_NAME = "rfinance_token"

/** Duração do cookie alinhada ao uso típico do token da API (7 dias). */
export const AUTH_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 7
